const initialState = {
  cart: [],
};

const cartReducer = (action, state) => {
  switch (action) {
    case "X":
      return state;

    case "Y":
      return state;

    case "Z":
      return state;

    default:
      return state;
  }
};

export default cartReducer;
